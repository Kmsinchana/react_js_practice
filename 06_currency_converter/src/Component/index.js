import InputBox from "./InputBox";

export {InputBox}

// we can use this file to export all the file in components {by using ,}
// This keeps your imports shorter, cleaner, and easier to maintain—especially when the folder contains many components.
// index.js acts like a root entry point
// we can specify only ./componts in app.js it will root to index.js file automatically